runtime: nodejs22
version: 

automatic_scaling:
  max_instances: 10

env_variables:
  NODE_ENV: production
  RECAPTCHA_SECRET_KEY: ${RECAPTCHA_SECRET_KEY}
  ADMIN_EMAIL: ${ADMIN_EMAIL}
  PHYSICAL_MAILING_ADDRESS: "APOD Email, P.O. Box 620572, Middleton, WI 53562, USA"
